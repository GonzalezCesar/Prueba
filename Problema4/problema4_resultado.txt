=== Análisis de código C ===

Palabras reservadas encontradas (6):
int, int, float, if, else, return

Traducción al español:
  int → entero
  float → flotante
  if → si
  else → si no
  return → retornar / devolver
